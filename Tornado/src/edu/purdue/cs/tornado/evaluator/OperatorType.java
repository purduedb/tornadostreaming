package edu.purdue.cs.tornado.evaluator;

public enum OperatorType {
	//GENERAL
	
	
	//INDEX
	SPATIAL_INDEX,
	TEXT_INDEX,
	HYBRID_INDEX,
	//SPATIAL
	SPATIAL_INSIDE,//arguments  true /false
	SPATIAL_DIST, //
	SPATIAL_OVERLAP,
	//Textual
	TEXT_DIST,
	TEXT_CONTAINS,
	TEXT_OVERLAP,
	TEXT_UNION,
	TEXT_INTERSECT,
	TEXT_DIST_ANY,
	TEXT_DIST_ALL,
	TEXT_SEMANTIC,
	TEXT_SENTIMENT,
	//Hybrid
	ORDER_BY,
	//GROUP
	SPATIAL_WITHIN_DIST,
	SPATIAL_PARTITIONS,
	
	//AGGREGATE
	SPATIAL_CENTROID,
	SPATIAL_DIAMETER,
	TEXT_FREQUENT,
	
	//JOIN
	JOIN
	
}
